{
  "name": "shelf-replenishment",
  "version": "1.0.0",
  "description": "A small Python project for calculating replenishment recommendations from inventory data using only the Python standard library.",
  "main": "script.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build": "node --check script.js",
    "test": "python -m unittest discover -s tests -q"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@zxing/browser": "^0.2.1",
    "xlsx": "^0.18.5"
  }
}
